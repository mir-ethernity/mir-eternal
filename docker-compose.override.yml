version: '3.4'

services:
  database:
    container_name: Mir3D_DB
    image: postgres:13
    ports:
      - 5432:5432
    volumes:
      - ./Database/Postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: mir-eternal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5

  adminpanel:
    container_name: Mir3D_AdminPanel
    image: strapi/strapi:latest
    restart: always
    environment:
      - DATABASE_CLIENT=postgres
      - DATABASE_NAME=postgres
      - DATABASE_HOST=database
      - DATABASE_PORT=5432
      - DATABASE_USERNAME=postgres
      - DATABASE_PASSWORD=mir-eternal
    volumes:
      - ./AdminPanel:/srv/app
      - ./Database/admin-uploads:/public/uploads
    ports:
      - 1337:1337
    depends_on:
      database:
        condition: service_healthy


  xaccountserver:
    ports:
      - 8001:8001/udp
    volumes:
      - ./Database/Account:/app/Accounts
    environment:
      - Database__Host=database
      - Database__Port=5432
      - Database__User=postgres
      - Database__Pass=mir-eternal
      - Database__DBName=postgres
      - LoginGatePort=8001
      - LoginServerPort=6678
      - Servers__0__Name=GS1
      - Servers__0__PrivateIP=xgameserver
      - Servers__0__PublicIP=127.0.0.1
      - Servers__0__Port=8701
    depends_on:
      xgameserver: 
        condition: service_started
      database:
        condition: service_healthy

  xgameserver:
    ports:
      - 8701:8701/tcp
    volumes:
      - ./Database/System:/app/Database/System
      - ./Database/GS1User:/app/Database/User
      - ./Database/GS1Backups:/app/Backups
    environment:
      - GSPort=8701
      - MaxLevel=60